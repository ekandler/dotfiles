#ifndef STRINGOPSTEST_H_
#define STRINGOPSTEST_H_

#include "StringOps.h"
#include "gtest\gtest.h"
#include <vector>

class StringOpsTest : public ::testing::Test {
public:
	StringOpsTest();
	virtual ~StringOpsTest();

	virtual void SetUp();
	virtual void TearDown();

protected:
	StringOps * mpObj;
	// result params for analyzeString
	size_t mChars;
	size_t mDigits;
	size_t mSpecialChars;

	// result params for crackUncPath
	std::string mHostname;
	std::string mShareFolder;
	std::vector<std::string> mPath;
	std::string mFilename;
	std::string mExtension;


};

#endif